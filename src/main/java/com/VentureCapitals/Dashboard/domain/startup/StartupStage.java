package com.VentureCapitals.Dashboard.domain.startup;

/** Company stage shown on profiles and used for discovery filters. Matches the frontend's STAGES. */
public enum StartupStage {
    PRE_SEED,
    SEED,
    SERIES_A,
    SERIES_B,
    SERIES_C_PLUS,
    GROWTH
}
